# 项目永久规则

## 语言

- 所有面向用户的说明文档、实施计划、规格和交付说明必须使用中文。
- 所有 Git 提交信息以及后续 GitHub 提交信息必须使用中文。
- 提交类型前缀可使用约定格式（例如 `feat:`、`fix:`、`test:`、`docs:`、`chore:`），但冒号后的标题和提交正文必须使用中文。
- 不重写本规则生效前已经存在的英文提交历史；所有新提交立即遵守本规则。

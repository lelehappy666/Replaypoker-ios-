# River Club 未完成功能续开发交接

**暂停时间：** 2026-07-22  
**开发分支：** `main`  
**总实施计划：** `docs/superpowers/plans/2026-07-22-本地功能补齐与赌场筹码动画实施计划.md`

## 一、今天已经完成

### 1. 牌桌体验阶段

- 单时钟赌场筹码飞行动画。
- 下注、底池、派奖与筹码堆高度联动。
- 筹码动画速度和牌桌音效节流。
- 快捷聊天、快捷表情和机器人有限回复。
- 局内设置即时保存；机器人设置从下一次行动或下一手生效。

### 2. 本地锦标赛存储与账本阶段

- 新增 `TournamentID`、`TournamentPhase`、`BlindLevel`、`TournamentSessionView`。
- 新增九人单桌赛事恢复状态：盲注级别、座位筹码、淘汰排名、真人座位。
- 聚合存档升级到版本 5；版本 1–4 自动迁移为空赛事集合。
- 新增幂等报名接口 `registerForTournament`，报名费与赛事状态原子保存。
- 新增幂等取消接口 `cancelTournamentRegistration`，仅开赛前允许全额退款。
- 新增幂等奖励接口 `awardTournamentPrize`，名次核对后一次派奖并结束赛事。
- 报名、退款、派奖全部进入娱乐筹码账本，并防止业务标识冲突和重复入账。

### 3. 已完成验证

以下定向回归共 26 项通过：

```bash
cd Packages/PokerCore
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
CLANG_MODULE_CACHE_PATH=/tmp/river-club-clang-cache \
SWIFTPM_MODULECACHE_OVERRIDE=/tmp/river-club-swiftpm-cache \
swift test --filter 'TournamentLedgerTests|TournamentSessionTests|SessionRecoveryTests'
```

覆盖内容：赛事账本、版本 5 迁移、旧版牌局恢复、历史记录身份保留与损坏存档拒绝。

## 二、明天从这里继续

### 第一优先级：Task 8 单桌锦标赛协调器

需要新增：

- `Packages/PokerCore/Sources/PokerCoordinator/Tournament/TournamentCoordinator.swift`
- `Packages/PokerCore/Sources/PokerCoordinator/Tournament/TournamentProjection.swift`
- `Packages/PokerCore/Tests/PokerCoordinatorTests/TournamentCoordinatorTests.swift`

需要完成：

1. 从已报名状态开始或从版本 5 存档恢复。
2. 复用现有 `HoldemEngine` 和机器人安全观察接口。
3. 保证行动顺序始终顺时针。
4. 盲注只在一手结束后升级，暂停期间不连续跳级。
5. 结算后淘汰零筹码座位；同手多人淘汰按开局前筹码确定名次。
6. 真人淘汰后进入待领奖或结束状态，冠军进入待派奖状态。
7. 为 `TournamentSession` 增加协调器需要的包内状态转换方法，不允许 SwiftUI 直接改真值。

### 第二优先级：Task 9 锦标赛界面与恢复流程

需要修改或新增：

- `RiverClub/Features/Tournaments/TournamentsView.swift`
- `RiverClub/Features/Tournaments/TournamentTableView.swift`
- `RiverClub/Features/Tournaments/TournamentResultView.swift`
- `RiverClub/App/AppSession.swift`
- `RiverClub/App/AppRootView.swift`
- `RiverClubTests/TournamentFlowTests.swift`

需要将当前临时的 `registeredIDs` 替换为 `LocalPokerStore.tournamentSessions`，并根据真实状态显示：报名、取消、进入赛事、继续赛事、查看结果。

### 后续阶段：Task 10–14

- 本地个人资料和成就存储。
- 昵称、内置真人头像和相册头像编辑。
- 成就页面和完成事件接入。
- 本地数据导出、重置与隐私说明。
- 全量回归、中文文档和最终交付。

## 三、继续开发时的关键约束

- 所有说明文档、计划和交付内容使用中文。
- 所有 Git 和 GitHub 提交信息使用中文。
- 继续直接在 `main` 开发。
- 赛事真值只允许由 `PokerSession` 和 `PokerCoordinator` 修改，SwiftUI 只消费投影状态。
- 机器人只能获得当前座位允许看到的 `PlayerObservation`，不能读取其他底牌、牌堆或未来结果。
- 每个账本命令必须在单个 `transact` 中保存，失败时不得发布半完成状态。
- 优先运行定向单元测试；界面微调不增加大量无价值 UI 测试。

## 四、建议的下一条提交

完成 Task 8 后使用：

```bash
git commit -m "feat: 实现本地单桌锦标赛协调器"
```
